[project]
name = "cc-template"
version = "0.1.0"
description = "Claude Code 项目工程模板生成工具"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]

[project.scripts]
cc-init = "scripts.init:main"
