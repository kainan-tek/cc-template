## 安全规范

### 编码准则

- 不硬编码密钥、密码、Token
- 输入校验：所有外部输入必须验证
- SQL 参数化：禁止字符串拼接 SQL
- 权限最小化：仅授予必要权限

### 依赖审计

- 定期运行依赖安全扫描
- 及时更新有漏洞的依赖
- 锁定依赖版本

### 敏感文件处理

- `.env` 文件不提交到版本控制
- 密钥文件（`.key`、`.pem`）不提交
- 使用 `.env.example` 记录所需环境变量
